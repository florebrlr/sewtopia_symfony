<nav class="navbar navbar-expand-lg">

    <div class="container-fluid">

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main_home') }}"
                       title="Retour sur la page d'accueil">
                        Accueil
                    </a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('pattern_list') }}"
                       title="Voir tous mes patrons">
                        Mes patrons
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('pattern_create') }}"
                       title="Ajouter un patron">
                        Ajouter un patron
                    </a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main_about_me') }}"
                       title="A propos de moi">
                        A propos de moi
                    </a>
                </li>


                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout')}}" title="Sign out">Se déconnecter (Hello {{ app.user.username }})</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register')}}" title="Register">S'enregistrer</a>
                </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login')}}" title="Sign in">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

